SHELL=/bin/bash

PROJECT_NAME=fckupio
BIND_TO=0.0.0.0
RUNSERVER_PORT=8000
MAILSERVER_PORT=1025

PYTHONPATH=$(CURDIR):$(CURDIR)/$(PROJECT_NAME)

PYTHON= PYTHONPATH=$(PYTHONPATH) python

MANAGE= $(PYTHON) $(PROJECT_NAME)/manage.py
